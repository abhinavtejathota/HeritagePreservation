//App-level constants
export const APP = {
  NAME: "Heritage Agent System",
  VERSION: "1.0.0"
};

//Orchestrator / NLP thresholds
export const ORCHESTRATOR = {
  INTENT_CONFIDENCE_THRESHOLD: 0.3
};

//Safety-related constants
export const SAFETY = {
  DEFAULT_BLOCK_MESSAGE: "Sorry, I can’t help with that request."
};

//Agent-related constants -> not in use right now
export const AGENTS = {
  DEFAULT_CONFIDENCE: 0.5
};

//API constants
export const API = {
  BASE_PATH: "/api",
  CHAT_PATH: "/chat"
};
