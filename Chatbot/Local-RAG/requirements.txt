# Prefer the repo-root requirements (one venv for the whole project):
#   pip install -r ../../requirements.txt
#
# This file remains for Local-RAG-only installs.
# GPU llama-cpp / CUDA torch: use scripts/install_gpu_llm.ps1 after the root install.
-r ../../requirements.txt
