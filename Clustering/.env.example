# Copy to .env and fill in (do not commit .env)
# Use the same DB as Application/backend/server/.env
PORT=8177
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=heritage
DB_PORT=5432
